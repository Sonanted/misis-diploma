# Database
DB_NAME=diploma-db
DB_USERNAME=postgres
DB_PASSWORD=change_me_strong_password

# JWT
JWT_SECRET=change_me_very_long_random_secret
JWT_EXPIRES_IN_MS=86400000

# Auth
SALT_ROUNDS=10
TOPUP_PASSWORD=change_me_topup_password

# CORS — set to your server's public URL (e.g. http://1.2.3.4 or https://example.com)
FRONTEND_URL=http://YOUR_SERVER_IP

# Set to true only when serving over HTTPS
COOKIE_SECURE=false
